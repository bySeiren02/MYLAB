{
  "name": "mylab",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "ios:open": "npm run ios:open --prefix frontend",
    "ios:build": "npm run ios:build --prefix frontend"
  }
}
